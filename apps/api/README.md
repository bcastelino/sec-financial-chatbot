# Filing Room API

FastAPI service for identified SEC access, deterministic Company Facts selection,
structured EdgarTools parsing, retrieval, and one-call grounded answer generation.

Run locally with `uv run uvicorn filing_room.main:app --reload`.
